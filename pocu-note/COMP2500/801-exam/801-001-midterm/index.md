아래 형태로 작성

question?
 - answer 


Java의 모든 함수는 클래스 내부에 작성
내포클래스 public 접근제어자 허용
- 접근제어자가 public인 최상위 클래스 2개 이상 컴파일 오류
메인 함수와 JVM의 진입점
- 동일한 클래스에 메인 함수 여러개 (지정된 시그니처) 불가능
- 여러 클래스에 각각 메인 함수(지정된 시그니처) 가능
메인 함수의 시그니처 강제는 실행 중 강제됨
- 올바른 시그니처를 사용하지 않으면, 메인 함수를 찾을 수 없다고 런타임 오류
- 컴파일 타임에 메인 함수의 존재 여부를 알 수 없음
커맨드 라인 인자 파싱
`System.out.println`
- System은 대문자로 시작하는 클래스
- out은 System 클래스의 public static 멤버변수, 타입은 `Printstream`
- `Printstream` 타입의 메서드 중 하나 println
	- println의 함수 오버로딩
printf
- 가변인자를 처리하는 원리
	- Object 배열
- String.format()

올바른 새 줄 문자 추가 방법

패키지 문법
패키지는 폴더 구조를 강제함
하위 패키지

커맨드 라인에서 자바 컴파일하기
- 패키지 구조에 따라 폴더 생성
커맨드 라인에서 자바 실행하기
- 포인트 2가지
	- 클래스 패스 지정
		- 자바 컴파일 명령어와 연관지어 생각
		- 패키지와 관련해 폴더 구조 개념을 이끌어내기
	- 메인 클래스 지정
		- 메인 함수를 포함하는 클래스를 지정
			- 메인 함수를 포함하는 클래스는 여러 개 존재할 수 있음
			- 커맨드 라인에서 지정은 오직 하나
		- 패키지와 관련해서 클래스 식별 이름 개념을 이끌어내기

커맨드 라인에서 jar 배포하기
커맨드 라인에서 jar 실행하기
- 커맨드 라인에서 자바를 실행하는 명령어 기억하나?
- Main 함수를 어떻게 알 수 있을까?
	- 매니패스트 파일

java.lang
- built in 패키지
- import가 필요없는 패키지

자바 실행 모델
- 자바 컴파일 모델을 알아야함
	- 전통적인 C 스타일의 컴파일과 비교
		- 실행 파일
		- 기계어
		- 플랫폼 의존
	- 바이트코드
	- JVM
		- JVM이라는 프로그램은 플랫폼에 의존
		- 바이트 코드를 기계어로 번역하는 역할
			- 인터프리터
			- JIT

자료형 시험 전에 한 번 읽고 들어가기

부호없는 자료형의 부재로 발생하는 문제
- 배열의 색인
- 음수 나이
- 색깔 표현
	- unsigned byte의 부재
	- 2바이트 short 강제라 낭비
- Integer Helper 함수

char
- 유일하게 부호없는 정수
- 16비트
	- 유니코드를 모두 표현하지 못함
		- String은 UTF-16 인코딩해서 저장

기본 자료형은 값형
- CPU에서 바로 연산 가능

String이란?
- 연속된 문자를 표현하는 자료형

String으로 유니코드 표현하려면?
- 코드 예시 기억 

String은 값형 vs 참조형?
- 클래스형
- String이라는 표기에서 참조형(클래스형)임을 찾는 힌트?
	- 대문자로 시작함

`new` 키워드로 String 생성하면 어떤 일 발생?
- 새로운 String 개체 생성
	- 문자열 매개변수 하나를 받는 String 생성자의 동작은?
		- 문자열 내용을 복사해 새로운 String 개체 생성
			- 엄밀히 말하면 `char[]` 참조를 공유하는 개념

String immutable 이란?
- 한 번 생성된 문자열의 내용을 바꿀 수 없음
	- 바꾸려 시도하면?
		- `[]`로 접근하면 컴파일 에러
		- 바꾸는 메서드는 새로운 문자열을 생성하는 방식

정수 리터널 l or L 이 반드시 필요한 순간?
- int 보다 큰 수를 우변에 표현할 때
- int 보다 큰 수를 long 변수에 대입해도 컴파일 오류

8진수 리터럴?
- 0으로 시작해서 착각 주의

String 리터럴로 유니코드 표현하기?
- `\u`

final 키워드가 붙을 수 있는 곳?
- 클래스 멤버 변수
- 지역 변수
- 메소드 매개변수
- 클래스 및 메소드

변수에 final 키워드가 붙을 때 효과?
- 값을 변경하려고 시도하면 컴파일 에러

java final 멤버 변수의 차별점(C, C#) 비교
- static 키워드를 붙일 수 있음

final 지역 변수를 컴파일 에러 없이 초기화 하려면?
- 선언과 동시에 초기화
- 사용하기 전 초기화

final 클래스 멤버 변수를 컴파일 에러 없이 초기화 하려면?
- 선언과 동시에 초기화
- 생성자에서 초기화
	- 다른 메소드에서 초기화 시도 시 컴파일 에러
	- 생성자는 개체 생성 시 반드시 초기화 됨을 기억

char을 더하기 위해 + 연산자 사용 가능함?
- 가능
- 정수형

boolean을 더하기 위해 + 연산자 사용 가능함?
- 불가능
	- 컴파일 에러
- 정수형 취급 X

long 변수를 int 변수에 대입하면 어떤 일 발생?
- 컴파일 에러
- 암시적 캐스팅은 작은 비트에서 큰 비트로 가능
- 명시적 캐스팅 필요

java string constant pool?
- String 변수가 동일한 문자열 리터럴을 공유하는 매커니즘

비교 연산자 `==`로 문자열을 비교할 때 메커니즘?
- 주소 비교
- 참조형(String 대문자로 시작하는 클래스형)

equals 함수로 문자열을 비교할 때 메커니즘?
- 내용 비교

연산자 오버로딩이란?
- 같은 연산자지만 동작이 다르다
- 피연산자의 자료형에 따라 연산자의 동작을 정의

문자열 더하기 연산자 사용시 발생하는 메커니즘?
- 새로운 개체 생성

switch 문의 표현식에 들어올 수 있는 자료형은?
- 정수형
	- byte, short, int, long, char
- enum
- String
	- equals로 내용비교

switch-case 문에서 case 블록에 break를 넣지 않으면?
- 문제 발생 X
- intentional fallthrough

`break <라벨이름>`과 goto의 차이점
- 라벨 블록 안에서만 사용할 수 있음

1차원 배열 선언 문법?
- 좌변
	- `int arr[]`
	- `int[] arr`
- 우변
	- `new int[5]`

다차원 배열의 우변에는 배열의 크기를 선언함. 이 때 꼭 필요한 크기는?
- 가장 바깥 배열의 크기
	- 안쪽 배열의 크기는 컴파일 시점 무의미
	- 런타임에만 의미있음
- jagged array

열거형에 `=`으로 대입 불가능한 이유?
- 클래스형이기 때문

열거형이 클래스형이라 제공하는 특수한 기능은?
- 멤버 변수
- 멤버 함수
- 대신 열거형 마지막에 `;` 필요
- 암시적 private 생성자로 멤버변수 초기화 가능
	- 생성자 호출 시점은?
		- enum 개체 생성할 때 
		- 매개변수의 값은 컴파일러가 넣어줌
- new 키워드로 enum 개체 생성 가능?
	- 불가능
	- 컴파일 에러
	- 정해진 enum 생성 문법으로 생성하면, 컴파일러가 알아서 생성자 호출 및 매개변수에 값 넣어줌
		- 이 때문에 enum 멤버에 괄호하고 값을 넘기는

var을 사용하면 컴파일 에러가 발생하는 곳?
- 배열의 타입으로 var 사용
- 중괄호 이용한 배열 초기화 문법 

패키지 시스템의 한계?
- 배포 파일의 용량이 커짐
	- 왜 배포 파일의 용량이 커짐?
		- 자바 빌트인 패키지를 모두 배포하는 불상사
			- 실행 중 사용하는 패키지를 알 수 없기 때문
- 패키지 안의 public 클래스를 패키지를 받는 사람은 모두 사용할 수 있음
	- 일부 클래스만 사용하게 하는 것이 불가능

모듈의 장점
- 패키지 시스템의 한계를 극복하니까
	- 패키지의 특정 클래스만 노출할 수 있음
		- 어떻게?
			- module-info.java 라는 파일에 써놓는 개념
				- 이 파일 때문에 얻는 모듈의 다른 장점?
					- 프로그램 시작 시 누락된 모듈을 확인할 수 있음
					- 모듈이 다른 어떤 모듈을 참조하는지 의존성을 찾을 수 있음
					- 배포 시 필요한 패키지만 배포해 경량화 가능
					- 노출하고 싶은 모듈만 노출할 수 있음

OOP 3대 특성
- 캡슐화
	- data hiding
	- 관련된 상태와 동작을 묶음
- 상속
	- 공통 코드 추출
	- 다형성을 위함
- 다형성
	- late binding

4대 특성에 추가되는 것
- 추상화
	- 데이터 추상화
		- 데이터에 바로 접근하는 것 X
		- getter 같은 함수로 접근
	- 클래스 추상화
		- 인터페이스, 추상 클래스로 추상화
		- 구체적인 클래스를 바로 아는 것이 아님

7대 특성에 추가되는 것
- 연관
	- 개체의 기능을 다른 개체가 사용하는 관계
	- 개체가 다른 개체를 참조
	- 상속 vs 컴포지션 에서 컴포지션으로 불리기도 함
- 집합
	- 연관을 집합과 컴포지션으로 나누는 관점
	- 어떤 개체가 다른 개체를 참조하는 관계, 참조 당하는 개체가 개별로 의미가 있음(사용 가능)
- 컴포지션
	- 연관을 집합과 컴포지션으로 나누는 관점
	- 어떤 개체가 다른 개체를 참조하는 관계, 참조 당하는 개체가 개별로 의미가 없음(부품으로 존재)

접근 제어자에서 외부와 내부의 기준?
- 클래스

접근 제어자 오류는 컴파일 시점 vs 실행 시점?
- 컴파일 시점

가비지 컬렉션의 대가?
- 메모리 해제 시점을 알 수 없음
	- 바로 하고 싶은데 못함
- 개체 사용 여부를 확인해 해제 안 한 메모리를 찾는 연산의 비용
- 가비지 컬렉션도 메모리 누수를 일으킴

생성자 오버로딩에서 중복 코드를 피하는 법
- 매개변수가 적은 생성자에서 매개변수가 많은 생성자를 호출할 수 있음
	- `this()`

기본 생성자
- 클래스에 생성자가 없으면, 컴파일러가 자동으로 만들어주는 생성자
	- 매개변수 없음
	- 바디 없음

생성자로 개체를 초기화하는 이유
- 함수의 후조건 그대로 적용
	- 개체는 생성되면 유효한 상태를 가짐
- 개념적으로 개체를 생성하고, 유효한 값을 대입하는 것이 이상함
- 클래스를 만든 사람이 클래스에 대해서 가장 잘 알기 때문에, 사용자를 배려해서 계약을 제공

접근제어자가 나온 배경?
- 개체의 유효한 상태와 관련해서
- 개체는 자신의 상태를 스스로 책임져야함
- 생성 시점의 올바른 상태는 생성자가 책임지고
- 개체의 상태에 대한 접근을 제어해서 개체가 스스로 상태를 책임지게 함

private 생성자 용도?
- 생성자는 멤버 함수
- private 멤버 함수의 용도를 그대로 따라감
- 공통의 로직을 분리하기 위함 (코드 중복 제거)

함수를 통해 데이터에 접근하는 것의 장점? (데이터 추상화를 getter로 달성할 때 장점)
- 호출할 때 새로 계산이 필요한 값을 반환하기 편함
- setter의 논리를 적용해 검증로직 등을 넣을 수 있음
- 함수에는 다형성을 적용할 수 있음

setter 베스트 프렉티스
- private 멤버 변수
- 생성자로 새로운 개체는 유효하도록 강제
- 개체의 상태를 스스로 책임지게 구현
- getter을 public 접근제어자 괜찮으나 참조형을 반환할 때 주의
