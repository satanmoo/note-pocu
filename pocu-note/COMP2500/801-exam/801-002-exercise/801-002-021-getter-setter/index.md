---
title: getter, setter
---
# getter, setter

## 멤버 변수를 public으로 두는 대신 함수(getter/setter)로 접근할 때의 장점 3가지는?

1. 다형성: 상속으로 메서드를 오버라이드해 다형적으로 동작 가능 (필드는 오버라이드 불가)
2. 계산된 값 제공: 값을 멤버 변수로 저장하지 않고 getter에서 필요할 때 계산해 반환 가능
3. setter에서 부가 로직 실행: 유효성 검사 등 추가 동작을 끼워 넣을 수 있음

- getter에는 데이터 추상화 개념이 녹아 있음
